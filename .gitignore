# Runtime artifacts of a deployed exercise. The logs and the process id
# files change on every restart and describe one machine only.
projects/*/logs/

# A stray Hermes plan file written into this working directory. It
# belongs to the agent, not to the course.
.hermes/

# Test fixtures copied from the live kanban board hold real task text.
teach/drafts/*.db
teach/drafts/*.db-wal
teach/drafts/*.db-shm
teach/drafts/.test-state-*.json
hermes-config/scripts/.blocked-watch-state.json
